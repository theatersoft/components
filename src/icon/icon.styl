@import base
@import colors
@import theme

:global
    .icon
        display inline-block
        line-height 0
        svg
            fill currentColor
            vertical-align middle
        &:not(.small) svg
            width 8rem
            height 8rem
        &.small svg
            width 5rem
            height 5rem
