@import base
@import colors
@import theme

color = color-black
opacity = 0.6

.overlay
    background-color color
    position fixed
    width 100%
    height 100%
    opacity 0
    pointer-events none
    transition opacity animation-duration animation-curve-default

    &.active
        opacity opacity
        pointer-events all
